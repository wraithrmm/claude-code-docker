{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp", "--browser", "chromium", "--headless", "--no-sandbox", "--isolated", "--ignore-https-errors", "--executable-path", "/ms-playwright/chromium-current/chrome-linux/chrome", "--output-dir", "/Users/claude-code/screenshots"]
    }
  }
}